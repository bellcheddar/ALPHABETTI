# ALPHABETTI, web tier. Deliberately light: this process never imports torch,
# never loads a model and never folds anything. See config.py for why.
Flask==3.1.3
gunicorn==23.0.0
requests==2.32.5
numpy==2.5.2
biopython==1.88
# freesasa is optional. It is the reference implementation and slightly
# faster, but it has no Linux wheel and needs a compiler. Biopython's
# Shrake-Rupley is used when it is absent and agrees to 0.1% (see
# alphabetti/accessibility.py). Uncomment only where a toolchain already exists.
#   freesasa==2.2.1

# HOGWASH. WebLogo does all the sequence-logo science; scipy is its dependency,
# not ours. PDF/PNG/JPEG additionally need the `ghostscript` package and SVG
# needs `pdf2svg`, both installed by deploy/provision.sh; without them EPS, CSV
# and logodata still work and the app says which are available.
weblogo==3.9.0
scipy==1.18.1

# Optional, and only for the `local` fold backend used to bulk pre-warm the
# cache from a machine that has the weights and about 16 GB of memory. NOT
# needed on the droplet, and installing it there would be a mistake.
#   torch>=2.4
#   transformers==4.46.3
#   accelerate
